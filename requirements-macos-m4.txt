# Minimal environment used for the macOS Apple Silicon reproduction path.
# Install PyTorch/PyG wheels that match the active Python version first when
# pip cannot resolve platform-specific sparse packages automatically.
torch>=2.11.0
torch_geometric>=2.7.0
torch_scatter>=2.1.2
numpy>=2.2.0
scipy>=1.15.0
scikit-learn>=1.7.0
tqdm>=4.67.0
matplotlib>=3.10.0
pandas>=2.2.0
